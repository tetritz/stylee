export declare class Switch {
    private _selector;
    constructor(selector: string);
    atest1(): Promise<void>;
    atest2(): Promise<number>;
    max(a: number, b: number): number;
    min(a: number, b: number): number;
    get selector(): string;
}
export default Switch;
