{
  "name": "aquamarine",
  "private": true,
  "scripts": {
    "git:push": "git add . && git commit -m 'Update codes' && git push"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {}
}
